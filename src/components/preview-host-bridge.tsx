/**
 * Next.js does not use the TanStack preview bridge. This remains as a no-op to
 * keep the component import stable while the app is migrated to the App Router.
 */

export function PreviewHostBridge() {
  return null;
}
